To do
=====

* tags, inclusion and exlusion, values
* allow repeats for forms, sections, items on the same row
* better parsing of tags and repeats
* inclusion of external defines / values
* fake subsections that can appear inside sections but not in the form
* an exclusion where all tags are included except for specific exc ones
